stages:
    - deploy

variables:
    LANG: "en_US.UTF-8"
    LC_ALL: "en_US.UTF-8"
    GIT_SUBMODULE_STRATEGY: recursive

beta:
    tags:
        - beta
    stage: deploy
    only:
        - master
    except:
        - tags
    script:
        - bash ./buildbox/beta.sh
    environment:
        name: internal
